# TRMS 铸造上下文

本上下文描述模具雕凿、填充、冷却与成型所使用的领域语言。它刻意不规定网络、方块实体或渲染实现；这些属于各端的实现决策。

## 模具与填充

**模具（Mold）**：可放置、可雕凿并可暂存一种填充材料的陶瓷容器。模具被挖掉后，填充材料不会随掉落物转移。

**凹腔（Cavity）**：模具中由雕凿形成、可被填充的连续空腔；其单元范围和连通规则沿用现有模具定义。

**填充材料（Fill material）**：用于表示凹腔当前内容的、可扩展的材料类型，例如铜或铁。一个模具在同一时刻只能有一种填充材料。

每种填充材料拥有稳定的命名空间 ID，例如 `trms:copper` 或 `trms:iron`。材料还定义可触发它的物品、显示材质和显示名称；模具保存材料 ID，而不是依赖固定的材料枚举。

**填充状态（Fill state）**：模具当前的材料状态，只有“未填充”或一种具体的熔融填充材料；它属于模具本身，并在模具仍被放置时保持。

**累计冷却 tick（Elapsed cooling ticks）**：已填充模具的权威冷却时间，取值为 `0..580`，只能是 20 的倍数。新浇筑为 `0`；每个已加载的服务端更新累加 `20`，第 30 次更新达到 600 tick 后完成成型。它不是秒数或离散视觉阶段。

**武器零件（Weapon part）**：一次完成冷却的单层铸件物品。它保存形成它的完整 `MoldPattern` 和 `Fill material`，其实体模型恰好是模具凹腔的单层闭合轮廓。当前它是后续武器装配的输入，不具有独立的攻击或右键行为。

填充状态只能从“未填充”转为一种材料；已填充的模具不会被另一种材料替换，也不会接受同种材料的重复填充。模具被挖掉并重新放置后，状态重新从“未填充”开始。

生存与冒险模式下，成功填充会消耗一个对应锭子；创造模式下不消耗。任何被拒绝的交互都不消耗物品。

填充发生后，当前凹腔的每个雕凿单元都会立即显示为连续的材料体积；液面略低于模具边缘，表现出尚未完全装满的余量。不模拟流动、延迟或逐格填充动画。未雕凿的陶瓷单元保持陶瓷外观，材料表现沿模具朝向旋转。

磁石只决定模具是否可雕凿，不是填充的前置条件；填充只要求模具处于有效放置状态。

空模具没有可填充的凹腔，因此不能接受填充；至少存在一个雕凿单元时，填充才是有效操作。

相邻的填充单元表现为连续的材料表面；材料内部的共享面不属于可见边界，因此不应产生单元缝隙。

填充暂时锁定雕凿；已填充模具不能继续雕凿，也不能在现有材料上追加空腔。冷却完成后，模具保留原有雕凿图案并重新变为可填充状态。任何尚未完成冷却的模具被挖掉时，雕凿图案可随模具物品保留，但填充材料和累计冷却 tick 不保留。

熔融填充使用项目自有、由原版岩浆动画帧导出的灰度动态纹理与岩浆发光语义；新浇筑的铜和铁均为 15 级光照。前 300 tick 保持满亮与原有火星、烟雾和熔岩环境音概率；从 300 tick 起，客户端由累计冷却 tick 推导视觉阶段，并按同一曲线降低方块光照、金属颜色亮度与特效发生概率。最后可见阶段仍有 1 级光照和 35% 色彩亮度。

这些材料是模具凹腔内的视觉特效，不是真正的流体；不会流向邻方块，也不改变模具现有碰撞或世界流体行为。

完成冷却的第 600 tick，服务端会在模具上方自动掉落一个 `trms:weapon_part`，并清除模具的填充和冷却状态。每次成功填充固定产出一个零件，不按雕凿面积折算成本。任何将模具转成物品的路径都只保留雕凿图案，不保留填充状态或累计冷却 tick；重新放置后模具必定未填充。

当前处于开发阶段，不为旧世界或旧模具数据提供兼容路径；`MoldFormat` 保持 v1，但已填充模具必须带有 `CoolingTicks`。旧的 `CoolingStage` 数据会被明确拒绝，不会静默重置为零 tick。

演示材料的输入分别是原版铜锭和原版铁锭；铜块、铁块、矿石及其他相似物品不触发填充。

填充顶面使用派生自 `lava_still` 的动画，侧面使用派生自 `lava_flow` 的动画；灰度基底使材料色调能够真正改变动态纹理的色相，并保持岩浆等级 15 的发光表现。

材料的显示色调由材料自身预定义并保持稳定；当前铜与铁分别使用固定的暖铜色和银灰色，不从运行时纹理采样推断。

雕凿、成功填充和每次累计冷却 tick 增加 20 都会使模具状态修订号递增；过期的客户端请求被拒绝。模具拆除后重新放置会创建新的状态生命周期，修订号从零开始。

_避免_：把“熔融填充材料”称为真实流体；它仍不流动或改变碰撞。只有完成冷却后掉落的武器零件才是铸件成品。
